{
  "name": "jobhuntbot",
  "version": "1.0.0",
  "private": true,
  "description": "AI-native job search system: JD research → evidence-based resumes → live job matching → local application dashboard.",
  "type": "commonjs",
  "scripts": {
    "init:workspace": "node scripts/init-workspace.js",
    "test:security": "node scripts/security-smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
